# Presidential Quiz App

Static Spanish-language quiz comparing policy preferences in Colombia.

## Run locally

```bash
python3 -m http.server 8000
```

Then open <http://127.0.0.1:8000/>.

## Deploy

This app has no build step. Serve the repo root as a static site.
